create database db1;
use db1;
create table registration (firstname varchar(30), lastname varchar(30), gender varchar(20), subs varchar(100), email varchar(30), phone varchar(20), cardname varchar(20), cardnumber varchar(20), cvv varchar(20));
desc registration;
select * from registration;
